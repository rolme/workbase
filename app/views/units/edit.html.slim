= render 'page_header', header: "Inventory: #{@unit.uuid}", unit: @unit, links: [:unit_index, :show_unit]

= render 'form', unit: @unit, unit_categories: @unit_categories, locations: @locations, url: nil
